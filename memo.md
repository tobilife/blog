# 블로그 개발 메모

## 2025-05-31 - 블로그 RAG 시스템 이슈

### 문제점
1. **성능 저하**
   - RAG 시스템 추가 후 504 Gateway Timeout 에러 빈발
   - API 응답 시간이 너무 느려짐

2. **검색 로직 버그**
   - 키워드 추출 후 검색이 제대로 작동하지 않음
   - "토비라이프의 경력사항" 검색 시 0개 결과

3. **Brave API 검색 기능 작동 안 함**
   - 웹 검색 기능이 동작하지 않음

### 임시 해결책
- RAG 시스템 전체를 임시 비활성화
- 원래 상태로 복구하여 성능 회복

### 향후 개선 방향
1. **더 가벼운 구현**
   - 프롬프트를 최소화
   - 불필요한 처리 제거

2. **검색 로직 개선**
   - Fuse.js 대신 더 단순한 검색 방식 고려
   - 키워드 매칭 로직 단순화

3. **점진적 적용**
   - 특정 키워드에만 제한적으로 적용
   - 성능 영향을 모니터링하며 점진적 확대

### 교훈
- 새 기능 추가 시 성능 영향을 반드시 고려
- 복잡한 시스템은 단계적으로 구현
- 실제 환경에서의 테스트 중요성

## 2025-06-01 - 대화 히스토리 최적화

### 문제점
- 대화가 길어질수록 Netlify 타임아웃 발생
- 8개의 대화 히스토리 전송으로 인한 성능 저하
- AbortError: signal is aborted 에러 빈발

### 해결 방법 (commit: ed81f10)
1. **대화 히스토리 개수 축소**
   - 8개 → 4개로 감소
   - 최근 대화만 유지하여 컨텍스트 효율성 향상

2. **메시지 길이 제한**
   - 각 메시지 최대 500자로 제한
   - 긴 메시지는 잘라서 '...' 추가

### 결과
- 전송 데이터 크기 약 60% 감소 예상
- 타임아웃 발생 빈도 감소 기대
- 사용자 경험 개선

### 다음 단계
- 성능 모니터링 후 추가 최적화 검토
- 필요시 스트리밍 응답 구현 고려
- 캐싱 전략 도입 검토

## 2025-06-01 - 웹 검색 기능 수정

### 문제점
- "오늘 뉴스 헤드라인 검색해줘" 같은 명시적 검색 요청에도 검색 기능이 작동하지 않음
- "검색 기능이 없다"라고 잘못된 응답
- 복잡도 분석에서 단순한 질문으로 분류되어 searchLimit이 0이 됨

### 해결 방법 (commit: de858ac)
1. **명시적 검색 요청 처리**
   - "검색해", "알려줘", "찾아" 등의 키워드 감지
   - 복잡도와 관계없이 최소 3개 이상의 검색 결과 보장

2. **검색 패턴 개선**
   - "뉴스 헤드라인", "헤드라인" 패턴 추가
   - "뉴스 검색" 패턴 추가

3. **프롬프트 및 응답 지침 개선**
   - 검색 결과 표시 형식 개선
   - "검색 기능이 없다"는 응답 방지 지침 추가
   - 뉴스 요약 지침 추가

### 결과
- 명시적 검색 요청 시 항상 웹 검색 수행
- 더 정확한 검색 의도 파악
- 사용자 경험 개선

## 2025-06-02 - OG Image 원본 복원

### 문제점
- OG 이미지 관련 여러 커밋을 통해 수정했으나 제대로 작동하지 않음
- 카카오톡, Facebook 등에서 이미지가 제대로 표시되지 않음
- 원본 Fuwari와 다르게 구현됨

### 해결 방법
1. **원본 Fuwari 코드로 복원**
   - 포스트 이미지를 OG로 사용하지 않고 항상 siteConfig.banner.src 사용
   - `banner = siteConfig.banner.src;` (TODO 주석과 함께)
   - og:image:width, og:image:height 태그 제거
   - url() 함수를 다시 사용

2. **원본 동작 방식**
   - 모든 페이지에서 동일한 배너 이미지 사용
   - 포스트별 커스텀 OG 이미지 미지원

3. **경로 수정**
   - config.ts에서 `src: "assets/images/banner.png"`로 변경
   - 원본 Fuwari와 동일하게 src 폴더 기준 상대 경로 사용
   - '/'로 시작하지 않는 경로로 설정

### 결과
- 원본 Fuwari와 동일한 OG 이미지 처리
- 일관된 소셜 미디어 프리뷰
- src/assets/images 폴더의 이미지 사용

- src/assets/images 폴더의 이미지 사용

## 2025-06-02 - 링크 프리뷰 이슈 해결

### 문제점
1. **페이스북 디버거 오류**
   - "잘못된 이미지 콘텐츠 유형" 오류
   - og:image URL(https://tobilife.netlify.app/assets/images/banner.png) 접근 불가
   - 빌드된 HTML에 og:image 메타 태그 누락

2. **원인 분석**
   - banner 변수가 null/undefined일 때 startsWith() 호출 시 에러 발생
   - 이로 인해 메타 태그 렌더링 실패
   - config.ts에서 banner.enable이 false로 설정되어 있었음

### 해결 방법
1. **메타 태그 안전 처리**
   - Layout.astro에서 조건부 렌더링 추가
   - `{banner && <meta property="og:image" .../>}`
   - twitter:image도 동일하게 처리

2. **config.ts 수정**
   - banner.enable을 true로 변경
   - mbanner.png를 public/images로 이동 완료

3. **빌드 캐시 정리**
   - dist 폴더 삭제 및 재빌드
   - 이전 빌드 결과물 제거

### 결과
- 메타 태그가 정상적으로 렌더링됨
- 페이스북/카카오 디버거에서 이미지 정상 표시
- 링크 프리뷰 기능 정상 작동

## 2025-06-02 - Tavily API 통합

### 목적
- 검색 결과 품질 향상을 위해 Brave API와 Tavily API를 병렬로 사용
- AI 최적화된 검색 결과 제공

### 구현 내용
1. **Tavily API 함수 추가**
   - searchTavily() 함수 구현
   - 무료 플랜 (월 1,000회) 사용
   - search_depth: 'basic' 설정

2. **병렬 검색 시스템**
   - performDualSearch() 함수로 두 API 동시 호출
   - 각 API별 3초 타임아웃 설정
   - Promise.all()로 병렬 처리

3. **결과 병합 알고리즘**
   - URL 기반 중복 제거
   - Tavily 결과 우선 (AI 최적화)
   - 스코어 기반 정렬
   - 최대 5개 결과 반환

4. **환경변수**
   - TAVILY_API_KEY 추가
   - Netlify 환경변수 설정 완료

### 장점
- 검색 결과의 다양성 확보
- AI 친화적인 응답 포맷
- 한 API 실패 시에도 서비스 가능
- 병렬 처리로 응답 시간 최소화